const Header = () => (
	<header className="w-full flex items-center justify-center p-4 bg-white shadow-md mb-8">
		<span className="text-2xl font-bold text-[var(--main-color)]">
			Freepilot
		</span>
	</header>
);

export default Header;
